{
  "name": "rolerocket-ai",
  "version": "1.0.0",
  "description": "RoleRocket AI job application assistant",
  "scripts": {
    "build": "cd backend && npm install",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": "18"
  }
}

